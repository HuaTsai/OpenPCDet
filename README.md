# OpenPCDet

Refactor OpenPCDet with pixi tool.

## Installation

```
curl -fsSL https://pixi.sh/install.sh | sh
pixi install
TORCH_CUDA_ARCH_LIST="8.9;9.0+PTX" pixi run pip install --no-build-isolation -e .
```

